#include <iostream>
using namespace std;
int main()
{

    // int a = 5;

    // cout << a << endl;

    // // print add of a
    // cout << &a << endl;

    // int* ptr = &a;
    // char ch = 'a';
    // char* cptr = &ch;
    // cout << "size of ch pointer is: " << sizeof(cptr) << endl;

    // // value of ptr means address of a
    // cout << ptr <<endl;

    // // access the value of address in ptr means
    // // ptr ke ander jo address hai us per konsi value hai
    // cout << *ptr << endl;

    // ---------------------------------------------------------

    // long a = 5;
    // long* ptr = &a;
    // cout << sizeof(ptr) << endl;

    // ===================================================

    // int* ptr = 0;
    // cout << *ptr ;    

    // int arr[] = {1,2};
  
    // int* p = arr;

    // int* q = arr + 4; // ==> garbage value or arr index out of bound

    // cout << *q ;


    // ===============================================
    // char ch[50] = "STATEMENT";
    // char* cptr = ch;

    // cout << (cptr + 2);
    // // cout << ch;

    int arr[] = {10,20,30};
    int* ptr = &arr;

   

    return 0;
}